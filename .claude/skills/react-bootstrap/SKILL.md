---
name: react-bootstrap
description: Scaffolds a minimal React 18 + TypeScript + Vite project per the user's prompt.
---

# react-bootstrap

Use when the user wants a new React component or app. Use functional
components + hooks (no class components). Prefer Vite over CRA. Default to
TypeScript unless the user explicitly asks for plain JS.
